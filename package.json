{
  "name": "muse-prints-storefront",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/admin dist/data dist/uploads && cp index.html styles.css app.js favicon.svg dist/ && cp admin/index.html admin/admin.css dist/admin/ && cp data/store.json dist/data/ && cp uploads/.gitkeep dist/uploads/ && esbuild src/admin.js --bundle --format=esm --minify --outfile=dist/admin/admin.js"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.12",
    "@netlify/functions": "5.3.0",
    "@netlify/identity": "1.2.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
